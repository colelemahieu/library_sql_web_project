<!DOCTYPE html>
<html>
<head>
    <link href="https://fonts.googleapis.com/css2?family=Libre+Baskerville:wght@700&display=swap" rel="stylesheet">
    <title>My Library</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            padding: 2rem;
	    background-color: #D6D6CE;
        }
        h1 {
            text-align: center;
            margin-bottom: 2rem;
        }
        .card-container {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 100px; 
            gap: 15px;
        }
        .card {
            background: white;
            border-radius: 10px;
            padding: 1rem;
	    flex: 1 1 150px;
            max-width: 150px;
            box-shadow: 0px 2px 6px rgba(0,0,0,0.1);
            text-align: center;
        }
        .card h2 {
            font-size: 1.2rem;
            margin-bottom: 0.5rem;
        }
        .card p {
            font-size: 1rem;
            color: #555;
        }
	.card img {
    max-width: 100%;
    height: auto;
    border-radius: 4px;
}
       .bookmark h1 {
    font-family: 'Libre Baskerville', serif;
    font-size: 1rem;
    margin: 0;

    /* rotate text */
    transform: rotate(-90deg);    
    transform-origin: center;     
    display: inline-block;         
}
       .bookmark {
    width: 200px;
    height: 75vh;
    background: url('https://www.transparenttextures.com/patterns/cloth-alike.png') repeat;
    background-color: #c0392b;  /* tint color under texture */
    color: white;
    padding: 10px 20px;
    text-align: center;
    position: absolute;
    top: 5vh;
    left: 10px;
    font-family: 'Libre Baskerville', serif;
    font-size: 1rem;
    border-radius: 0 0 5px 5px;
    z-index: 1000;
    box-shadow: 0 4px 6px rgba(0,0,0,0.2);
}

/* Make notch with same texture */
.bookmark::after {
    content: "";
    position: absolute;
    bottom: -20px;
    left: 50%;
    transform: translateX(-50%);
    width: 40px;
    height: 20px;

    background: url('https://www.transparenttextures.com/patterns/cloth-alike.png') repeat;
    background-color: #c0392b;

    clip-path: polygon(50% 100%, 0 0, 100% 0);
}
      
    </style>
</head>
<body>
    <div class="bookmark">
        <h1>Recently Read</h1>
    </div>    

    <div class="card-container">
        {% for book in recent_books %}
        <div class="card">
            <img src="{{ book['cover_url'] }}" alt="Cover of {{ book['title'] }}">
	    <h2>{{ book['title'] }}</h2>
            <p>{{ book['author'] }}</p>
        </div>
        {% endfor %}
    </div>
</body>
</html>

